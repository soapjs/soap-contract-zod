{
  "name": "@soapjs/soap-zod",
  "version": "0.1.1",
  "description": "Zod HTTP contracts — one schema for validation middleware and OpenAPI fragments",
  "homepage": "https://docs.soapjs.com",
  "repository": "https://github.com/soapjs/soap-zod",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build",
    "README.md"
  ],
  "license": "MIT",
  "author": "Radoslaw Kamysz",
  "scripts": {
    "test:unit": "jest --config=jest.config.unit.json",
    "test": "npm run test:unit",
    "clean": "rm -rf ./build",
    "build": "npm run clean && tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@soapjs/soap": "^0.14.0",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.14",
    "@types/node": "^18.7.14",
    "express": "^4.18.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "@soapjs/soap": ">=0.14.0",
    "express": ">=4.0.0",
    "zod": ">=4.4.3"
  }
}
