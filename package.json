{
  "name": "@soapjs/soap-contract-zod",
  "version": "0.1.1",
  "description": "Zod HTTP contracts — one schema for validation middleware and OpenAPI fragments",
  "homepage": "https://docs.soapjs.com",
  "repository": "https://github.com/soapjs/soap-contract-zod",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "license": "MIT",
  "author": "Radoslaw Kamysz",
  "scripts": {
    "test:unit": "jest --config=jest.config.unit.json",
    "test": "npm run test:unit",
    "clean": "rm -rf ./build",
    "build": "npm run clean && tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@soapjs/soap": "^0.12.1",
    "@types/express": "^4.17.21",
    "@types/jest": "^27.0.3",
    "@types/node": "^18.7.14",
    "express": "^4.18.0",
    "jest": "^27.4.5",
    "ts-jest": "^27.1.3",
    "typescript": "^4.8.2",
    "zod": "^3.25.76"
  },
  "peerDependencies": {
    "@soapjs/soap": ">=0.12.1",
    "express": ">=4.0.0",
    "zod": ">=3.22.0"
  }
}
